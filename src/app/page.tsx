import { LoginButton } from '../domain/LoginButton'

const Home: React.FC = () => (
  <div className="flex min-h-full items-center justify-center">
    <LoginButton />
  </div>
)

export default Home
